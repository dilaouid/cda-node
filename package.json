{
    "name": "cda-node",
    "private": true,
    "scripts": {
      "dev": "concurrently \"pnpm --filter client dev\" \"pnpm --filter server dev\"",
      "build": "pnpm -r build"
    },
    "devDependencies": {
      "concurrently": "^8.0.1"
    }
}